# website name
title: M. L. M. Fouesneau
# website URL
baseURL: 'https://mfouesneau.github.io'

#theme
theme: "default"
# content directory
sourcedir: "content"
# Anything to add to the build into a static folder
staticdir: "static"
# build directory
builddir: "_build/html"

# setup markdown options
markdown:
  # https://python-markdown.github.io/extensions/
  extensions:
    - extra